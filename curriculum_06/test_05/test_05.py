# 06章 05. 関数の定義と呼び出し・引数と戻り値・スコープ - 確認テスト（問題文）
# 年齢判定システムと、バグのある関数の修正問題です。
# [①]〜[⑤]の（）に入るコードを考えて、answers_05.txt に番号ごとの答えを記入してください。
# 答え合わせが終わったら、完成したコードを shakyou_05.py に1文字ずつ書き写して実行してください。

# ===年齢判定システム===
# 年齢を受け取り、18歳以上ならTrue、それ以外はFalseを返す関数を完成させる

# [①] 引数名を入れる
def is_adult（）:
    # [②] 18歳「以上」の比較演算子
    if age （） 18:
        # [③] 条件を満たした場合に返す値
        return （）
    else:
        return False

# テスト用データを定義する
user_age = 16

# [④] 定義した関数名で呼び出す
check_result = （）(user_age)

# 結果を表示する
print("成人判定の結果:", check_result)


# ===================================================
# バグ修正問題
# ===================================================
# 以下のコードを実行しても、画面に何も表示されません。
# [⑤] 足りない処理を1つ追加して、結果が画面に表示されるようにしてください

def multiply_by_two(number):
    result = number * 2
    return result

（）(multiply_by_two(10))
