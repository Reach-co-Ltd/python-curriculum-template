============================================================
curriculum_05 / SQL基礎 記述テスト
回答をこのファイルに直接記入して提出してください
============================================================

【問1】RDBMSとは何ですか？
回答:

【問2】以下のSQL文が何をするか説明してください。
  SELECT * FROM tasks WHERE done = 0
回答:

  INSERT INTO tasks (title) VALUES ("買い物")
回答:

  UPDATE tasks SET done = 1 WHERE id = 3
回答:

  DELETE FROM tasks WHERE id = 5
回答:

【問3】PRIMARY KEY の役割を説明してください。
回答:

【問4】SQLAlchemy を使うメリットを2つ挙げてください。
回答:
============================================================
